
  # Landing Page for Game Top-Up

  This is a code bundle for Landing Page for Game Top-Up.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  